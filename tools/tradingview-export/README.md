# TradingView exporter

Run `export-pine-scripts.js` in the browser console while logged into TradingView.

Acceptance condition:

```text
discovered == exported
failed == 0
```

The script is read-only and does not modify Monaco or save scripts. See `docs/EXTRACTION.md`.
