# Active core sources

This directory is intentionally empty at the foundation stage.

A core script is promoted from `archive/sources/core/` only after its first reboot blockers are resolved and a `0.1.0` candidate reaches the gates in `docs/TESTING.md`.

First target: **SignalGate Dashboard**.
