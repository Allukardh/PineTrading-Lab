# Active core sources

Files in this directory are reboot candidates/active sources. Historical originals remain immutable under `archive/sources/core/`.

A candidate may exist here before it is accepted, but it is not considered canonical production behavior until its promotion gates in `docs/TESTING.md` pass and its PR is merged.

| Script | Candidate | Milestone | Status |
|---|---:|---|---|
| SignalGate Dashboard | 0.1.0 | SG-0 deterministic timeframe hardening | Static PASS; TradingView compile/runtime pending |

All remaining core scripts still exist only in the immutable archive.
